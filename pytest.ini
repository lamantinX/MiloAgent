[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q
markers =
    live: tests that call real external services (deselected by default)
    integration: tests that exercise multiple internal components together
